import { TextSecurePipe } from './text-secure.pipe';

describe('TextSecurePipe', () => {
  it('create an instance', () => {
    const pipe = new TextSecurePipe();
    expect(pipe).toBeTruthy();
  });
});
